export interface DataUsuarios {
  empresa: string;
  idusuario: number;
  nombre: string;
  tusuario: number;
  usuario: string;
}

export interface DataLogin {
  empresa: string;
  nfantasia: string;
  tusuario: number;
  usuario: string;
}

export interface LoginFormData {
  empresa: string;
  password: string;
  usuario: string;
}
