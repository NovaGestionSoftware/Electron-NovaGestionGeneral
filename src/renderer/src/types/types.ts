export interface DataLogin {
  empresa: string;
  nfantasia: string;
  tusuario: number;
  usuario: string;
}

export interface LoginFormData {
  empresa: string;
  password: string;
  usuario: string;
}
